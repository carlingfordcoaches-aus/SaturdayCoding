"""Classes & Objects

Focus: Use classes to encapsulate behavior."""


def exercise_1():
    """**Method Chaining**: Add a `level_up` method to the `Lesson` class that upgrades the difficulty and returns `self` to allow for method chaining."""
    # TODO: Add a level_up method that upgrades the difficulty and returns self for chaining.
    pass


def exercise_2():
    """**Subclassing**: Create a `VideoLesson` subclass that inherits from `Lesson` and adds a `runtime_minutes` attribute. Override the `describe` method to include the runtime."""
    # TODO: Create a subclass VideoLesson that stores runtime minutes and overrides describe.
    pass


def main():
    """Uncomment the exercises you want to run."""
    # exercise_1()
    # exercise_2()
    pass

if __name__ == '__main__':
    main()
